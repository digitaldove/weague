import type { Component } from "solid-js";

/**
 * Placeholder for AppIcon SVG
 * @returns
 */
const AppIcon: Component = () => {
  return (
    <div class="flex flex-col">
      Weague
      <div class="font-mono text-xs">(Wordle + League)</div>
    </div>
  );
};

export default AppIcon;
